---
name: grill-me
description: A relentless interview to sharpen a plan or design, which also creates docs (ADRs and glossary) as we go.
disable-model-invocation: true
---

Run a `/grilling` session, using the `/domain-modeling` skill.

If `$HOME/repos/CLAUDE.md` points at a sandbox context repo, also use the `/system-mapping` skill to keep that sandbox's cross-repo notes current.
